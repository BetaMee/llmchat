# ============================================================
# Figma JSON 微调训练框架依赖
# 适配 PyTorch 2.10.0 + CUDA 12.8 + Python 3.13
# ============================================================

# -------------------- 核心训练库 --------------------
# PyTorch 深度学习框架
# 注意: 请根据您的 CUDA 版本安装对应的 PyTorch
# 版本：torch=2.10.0+cu128 文档是 https://pytorch.org/
# CUDA 12.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch==2.10.0
torchvision==0.25.0

# 注意：需要先安装指定 CUDA 版本的 PyTorch，再安装 Unsloth
# Unsloth - 高速微调加速库 (2-5x 提速)
# pip install unsloth
unsloth==0.1.0

# Transformers - Hugging Face 模型库 (支持当前 Qwen 模型)
transformers>=4.40.0

# Datasets - 数据集加载和处理
datasets>=2.18.0

# Accelerate - 分布式训练加速
accelerate>=0.27.0

# PEFT - 高效参数微调 (LoRA)
peft>=0.10.0

# TRL - Transformer 强化学习库
trl>=0.8.0

# Bitsandbytes - 4-bit/8-bit 量化
# CUDA 12.8 需要 0.43.0+
bitsandbytes>=0.43.0

# -------------------- 数据处理 --------------------

# ModelScope - 国内模型下载和使用（替代 HuggingFace）
modelscope>=1.11.0

# YAML 配置文件解析
pyyaml>=6.0.1

# 数据分析和处理
pandas>=2.2.0
numpy>=1.26.0

# 进度条
tqdm>=4.66.0

# -------------------- 训练监控 --------------------

# Weights & Biases - 实验跟踪 (可选)
wandb>=0.16.0

# TensorBoard - 训练可视化
tensorboard>=2.15.0

# 机器学习工具
scikit-learn>=1.4.0

# -------------------- 开发工具 (可选) --------------------

# 交互式 Python 环境
ipython>=8.20.0

# Jupyter Notebook
jupyter>=1.0.0

# -------------------- 推荐的额外依赖 --------------------

# Flash Attention 2 - 加速注意力计算 (推荐安装)
# pip install flash-attn --no-build-isolation

# Triton - GPU 编程语言 (Unsloth 可能需要)
# pip install triton>=2.0.0

# -------------------- vLLM 服务部署 --------------------
# 建议单开一个 conda python 环境安装 vLLM

# vLLM - 高性能推理引擎
# pip install vllm
vllm>=0.15.1

# FastAPI - Web 框架
# pip install fastapi uvicorn
fastapi>=0.131.0
uvicorn>=0.41.0

