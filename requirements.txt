# Unsloth - Fast finetuning library
# Note: For macOS, use basic unsloth without [colab-new] due to bitsandbytes compatibility
unsloth @ git+https://github.com/unslothai/unsloth.git

# Core dependencies
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.42.0,<0.43.0  # macOS 最高支持 0.42.0

# Training utilities
wandb>=0.16.0
tensorboard>=2.15.0
scikit-learn>=1.4.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
tqdm>=4.66.0

# Optional
ipython>=8.20.0
jupyter>=1.0.0

# Evaluation tools
openai>=1.0.0  # 用于评估脚本
